polkit.addRule(function(action, subject) {
    if (action.id == "org.zaone.setup" &&
        subject.local &&
        subject.active &&
        subject.isInGroup("zaone-setup")) {
        return polkit.Result.YES;
    }
});
